name: Build y Release NexoKit R10

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  notice:
    name: Preparación R10
    runs-on: ubuntu-latest
    steps:
      - name: Aviso
        run: echo "Workflow temporal durante la integración R10."
